# pharmax 0.3.0-public

- Reset public registry to the current proof slice: `pharmax`, `pharmax.viz`,
  and `pharmax.ml`.
- Added `px_public_packages()` and `px_public_status()` for public scope checks.
